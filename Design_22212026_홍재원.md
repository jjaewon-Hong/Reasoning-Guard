# Reasoning Guard

딥러닝 기반 미확인 공중 표적 정밀 판별 시스템





![Reasoning Guard Logo](./assets/Logo.png)





22212026 홍재원 hongjaewon0428@gmail.com



## Revision history

### Inference Engine
| Revision date | Version # | Description | Author |
| :---: | :---: | :---: | :---: |
| 03/17/2026 | 1.0.1 | 데이터셋 구축 | 홍재원 |
| 03/24/2026 | 1.0.2 | Zero-Padding 및 fail-Safe 설계 | 홍재원 |
| 03/30/2026 | 1.0.3 | ResNet-18모델 설계 및 지도학습 최적화 모듈 구축 | 홍재원 |
| 04/03/2026 | 1.0.4 | Reasoning Guard 총괄 파이프라인 구현 및 ONNX 추출 | 홍재원 |
| 04/30/2026 | 1.0.5 | 확률 도출 로직 추가 및 C++ 연동용 ONNX 엔진 업데이트 | 홍재원 |
| 05/03/2026 | 1.0.6 | 비정상 파일 유입 차단을 위한 확장자 필터링 적용 및 파이프라인 주석 정비(최종) | 홍재원 |

### Tactical Logic
| Revision date | Version # | Description | Author |
| :---: | :---: | :---: | :---: |
| 05/03/2026 | 1.0 | 이분법적 요격 판정(Shoot/Hold)을 신뢰도 기반 3단계 <br> (SAFE/CAUTION/DANGER) 전술 대응 로직으로 세분화 | 홍재원 |




## Contents

1. Introduction



2. Class Diagram  



3. Sequence Diagram 



4. State Machine Diagram 



5. Implementation Requirements 



6. Glossary



7. References



<br>



---

## 1. Introduction

본 설계서는 딥러닝 기반 미확인 공중 표적 정밀 판별 시스템인 "Reasoning Guard" 개발 프로세스의 제3단계 상세 설계(Design) 명세를 기술함. 선행된 Analysis 단계의 요구사항을 바탕으로 실제 시스템 구현에 관여하는 물리적·논리적 제반 요소의 윤곽을 확정하고 구체적인 아키텍처 설계를 수립함을 목적으로 함. 본 시스템은 단순 객체 탐지 알고리즘의 한계를 탈피하여 정밀 영상 분석 및 기하학적 형상 보존 기술(Zero-Padding)을 융합함으로써, 전장 노이즈에 기인한 불확실성을 능동적으로 감지하는 'Fail-safe' 메커니즘의 구현을 핵심 설계 원칙으로 함. 학습 도메인에서는 Python 및 PyTorch 기반의 객체 지향 모델 아키텍처를 구축하며, 도출된 신경망은 ONNX 포맷 변환을 거쳐 운용 도메인인 C++ 기반 고속 추론 엔진 환경에 최적화 탑재됨. 이를 통해 극한의 실시간 시계열 데이터 환경하에서도 표적 식별의 신뢰성을 확보하고, 궁극적으로 오발사 리스크 차단 및 국방 방공망 운용의 안정성 극대화를 도모함.

<br>

---


## 2. Class Diagram

![class_diagram_Training](./assets/_3_analysis/class_diagram_Training.png)

### 2.1 학습(Training) 관점 클래스

**(1) DatasetManager**
* **설명**: 클래스별로 규격화된 수량의 샘플을 무작위 추출하여 균형 잡힌 학습 데이터셋을 구축하고 관리함.
* **Attributes (속성)**
  * `- data_dir: string` : 데이터가 저장된 디렉토리 경로
  * `- samples_per_class: int` : 클래스당 추출할 샘플 수 (500개)
* **Operations (메서드)**
  * `+ build_path_list(): list` : 메모리 최적화를 위한 데이터 경로 리스트 생성
  * `+ split_train_val(ratio): void` : 학습용과 검증용 데이터 분할
* **Relationships (관계)**
  * `DataAugmentor` 객체를 집합(Aggregation) 관계로 포함하여 데이터를 가공함.

**(2) DataAugmentor**
* **설명**: 전장 상황의 다양성을 반영하기 위해 이미지 회전 및 반전 등의 기법으로 데이터를 증강함.
* **Attributes (속성)**
  * `- rotation_range: float` : 이미지 회전 적용 범위
  * `- horizontal_flip: bool` : 좌우 반전 적용 여부
* **Operations (메서드)**
  * `+ apply_transforms(): Compose` : 정의된 증강 기법들을 파이프라인으로 구성하여 반환

**(3) ResNet18_Classifier**
* **설명**: 항공 표적의 시각적 특징 추출을 위해 정의된 심층 신경망 아키텍처 모델임.
* **Attributes (속성)**
  * `- num_classes: int` : 최종 판별할 표적 클래스의 개수
  * `- pretrained_weights: weights` : 전이 학습을 위한 사전 학습 가중치
* **Operations (메서드)**
  * `+ forward(x): Tensor` : 입력 텐서에 대한 순전파 연산 수행
  * `+ get_feature_map(): Tensor` : 합성곱 레이어를 통과한 특징 맵 반환

**(4) ModelTrainer**
* **설명**: 손실 함수와 최적화 알고리즘을 사용하여 신경망 모델을 훈련하고 가중치를 업데이트함.
* **Attributes (속성)**
  * `- learning_rate: float` : 학습률 설정값
  * `- epochs: int` : 전체 데이터셋에 대한 반복 학습 횟수
  * `- opt: optimizer` : 모델 최적화 알고리즘 (예: Adam)
  * `- criterion: loss_fn` : 손실 함수
* **Operations (메서드)**
  * `+ train_step(): void` : 1 에폭 단위의 학습 수행
  * `+ validate_step(): void` : 모델 성능 검증 수행
  * `+ save_checkpoint(): void` : 특정 시점의 모델 상태 및 가중치 저장
* **Relationships (관계)**
  * `DatasetManager`를 사용(Uses)하여 데이터를 공급받음.
  * `ResNet18_Classifier`를 최적화(Optimizes)함.
  * 학습 완료 후 `ONNX_Exporter`를 트리거하여 모델 변환을 요청

**(5) ONNX_Exporter**
* **설명**: Python(PyTorch)에서 학습이 완료된 모델을 실시간 추론을 위한 ONNX 포맷으로 변환함.
* **Attributes (속성)**
  * `- output_name: string` : 추출될 ONNX 파일의 이름
  * `- config: dynamic_axes` : 가변 배치 사이즈 처리를 위한 설정
* **Operations (메서드)**
  * `+ convert(pytorch_model): void` : PyTorch 모델을 ONNX 형식으로 변환
  * `+ verify_integrity(): bool` : 변환된 모델의 무결성 및 정상 작동 여부 검증

  ![class_diagram_Operational](./assets/_3_analysis/class_diagram_Operational.png)

### 2.2 운용(Operational) 관점 클래스

**(1) ReasoningGuardSystem**
* **설명**: 전체 시스템의 추론 및 전술 파이프라인 흐름을 제어하는 최상위 컨트롤러 시스템임.
* **Attributes (속성)**
  * (명시된 속성 없음 - 시스템 제어 역할에 집중)
* **Operations (메서드)**
  * `+ run_pipeline(): void` : 전처리, 추론, 전술 판단으로 이어지는 메인 파이프라인 실행
  * `+ handle_exception(): void` : 파이프라인 진행 중 발생하는 예외 및 오류 처리
* **Relationships (관계)**
  * `ImagePreprocessor`, `InferenceEngine`, `TacticalDecisionMaker`, `COP_Interface`와 연관(Association) 관계를 맺고 파이프라인 순서대로 호출

**(2) ImagePreprocessor**
* **설명**: 입력된 원본 표적 이미지의 기하학적 형상 왜곡을 방지하기 위해 규격화 및 전처리를 수행함.
* **Attributes (속성)**
  * `- target_size: int` : 모델 입력에 맞춘 목표 이미지 해상도
  * `- use_padding: bool` : 종횡비 유지를 위한 Zero-Padding 적용 여부
* **Operations (메서드)**
  * `+ transform(raw_image): Tensor` : 모델 추론에 적합한 텐서 형태로 이미지 변환
  * `+ zero_padding(image): Image` : 여백에 0을 채워 넣는 패딩 작업 수행

**(3) InferenceEngine**
* **설명**: C++ 기반 운용 환경에서 ONNX 런타임을 활용하여 고속 경량 추론을 수행함.
* **Attributes (속성)**
  * `- model_path: string` : ONNX 모델 파일이 위치한 경로
  * `- confidence_threshold: float` : 유효한 판별로 인정하기 위한 최소 신뢰도 임계값
  * `- onnx_runtime: session` : 추론을 실행하기 위한 ONNX 세션 객체
* **Operations (메서드)**
  * `+ load_model(path): void` : 지정된 경로에서 모델을 메모리에 적재
  * `+ predict(tensor): float[]` : 전처리된 텐서를 입력받아 추론 결과(확률 배열) 반환
  * `+ get_top_k(probabilities): dict` : 가장 확률이 높은 상위 k개의 클래스와 신뢰도 도출
* **Relationships (관계)**
  * 도출된 신뢰도(Confidence Score)를 `TacticalDecisionMaker`로 전달(의존 관계)

**(4) TacticalDecisionMaker**
* **설명**: 추론된 신뢰도를 바탕으로 3단계(SAFE/CAUTION/DANGER) 로직 및 Fail-safe 대응을 수행함.
* **Attributes (속성)**
  * `- danger_boundary: float` : DANGER 단계(즉각 교전)로 판정하기 위한 임계값
  * `- safe_boundary: float` : SAFE 단계(아군/비위협)로 판정하기 위한 임계값
* **Operations (메서드)**
  * `+ evaluate(confidence_score): string` : 신뢰도를 기반으로 최종 전술 상태 평가 및 반환
  * `+ apply_fail_safe(status): void` : 불확실성 감지 시 오발 방지를 위한 Fail-safe 가동
* **Relationships (관계)**
  * 결정된 최종 전술 전략(Final Strategy)을 `ReasoningGuardSystem`으로 반환

**(5) COP_Interface**
* **설명**: 종합 전술 상황도(COP) 시스템과 연동하여 시각적 모니터링 및 경보 기능을 제공함.
* **Attributes (속성)**
  * `- monitoring_view: canvas` : 화면에 전술 상황을 출력하기 위한 뷰어 화면
  * `- history_log: list` : 누적된 판별 결과 및 전술 기록 로그
* **Operations (메서드)**
  * `+ update_display(result): void` : 추론 및 전술 판단 결과를 화면에 실시간 업데이트
  * `+ trigger_alarm(level): void` : DANGER 단계 등 위협 감지 시 시청각적 경보 발생
